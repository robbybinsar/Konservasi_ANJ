output <- data.frame(matrix(ncol=7,nrow=7))
colnames(output) <- c("Amfibi","Aves", "Insekta", "Mamalia","Myriapoda","Pisces","Reptil")
rownames(output) <- c("ANJA","ANJAS","SMM","KAL","PPM","PMP","ANJAP")
input <- function(x) {
  ## x adalah nama data framenya, data frame harus di WD
  dat <- read.csv(x)
  for(i i
    
  }
}

length(which(x[,index kolom]=="Taxon"))
##di atas ini untuk cari berapa